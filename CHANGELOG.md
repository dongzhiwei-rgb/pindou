<!--
文件：CHANGELOG.md
用途：记录拼了个豆的迭代更新（功能、修复、安全、性能、部署）。
核心职责：按日期归档功能变更、缺陷修复、兼容性处理与验证结果，提供可追溯的发布依据。
版权：@董志伟-联系方式-makabak1204
最后修改：2026-09-11
说明：每次改动按日期追加；格式为「日期 / 分类 / 变更内容（涉及文件）」，便于回溯。
-->

# 拼了个豆 · 变更记录

## 2026-09-11（移动端项目操作排序）

1. “保存”和“清空豆板”调整为项目功能区最前方的连续操作；常规手机双列布局中并排显示，极窄屏单列布局中紧邻排列，并继续使用品牌绿与危险红建立清晰区分。
2. 文件操作按主题语义重新强调：熨烫预览使用暖黄色、导入使用薰衣草紫、导出使用品牌绿色，禁用和悬停状态保持统一且不与红色危险操作混淆。
3. 手机端图纸生成抽屉改为内容驱动高度：短步骤自动收拢，长步骤在顶部安全区与底部导航之间尽量扩展，仅在达到屏幕上限后滚动内部内容。

4. 移动端全屏入口下移到缩放控制区域右侧，两者共用底部基线并保持视觉中心水平对齐。

5. 移动端顶部在“生成”按钮左侧新增常驻“授权与试用”入口，复用原授权弹窗与状态语义；桌面端原动态授权状态按钮保持不变，极窄屏同步收紧间距避免导航换行，并修复通用按钮样式覆盖隐藏规则导致桌面同时出现两个授权入口的问题。

6. 开屏动画总时长由 2.5 秒缩短为 1.5 秒，豆子汇聚、文字入场、高光扫过和工作台交叉淡入按新时间轴同步压缩；倒计时保持整数“2、1”显示，避免动画未完成即硬切。

7. PC 端“功能”弹窗由单列长菜单改为项目、显示、功能、文件四组 2×2 紧凑网格，组内命令使用双列并让奇数末项自动占满整行；常规桌面高度无需滚动即可完整显示，极矮窗口保留隐藏滚动条的滚轮访问兜底。

8. PC 功能弹窗四宫格统一采用顶部对齐，较少按钮的“项目/文件”分组不再在等高区域内垂直居中；四组小标题与首行按钮保持一致的顶部节奏。

9. 修复 821～1180px 窄桌面下左侧工具栏右边缘被裁切约 6px：工具轨外层宽度、72px 工具卡和两侧留白改用一致的 82px 网格占位，生成侧栏打开时沿用相同尺寸。

10. 移动端“生成图纸”弹窗固定使用安全区与底部导航之间的最大可用高度，不再随不同步骤的内容长度反复伸缩；超出内容改在弹窗内部滚动，切换步骤时弹窗边界保持稳定。

11. 生成 Linux Docker Compose 源码部署包 `pindou-studio-linux-20260911-v1.tar.gz`，同步更新部署文档中的包名，并提供 SHA-256 校验文件；发布包不包含本地管理令牌、数据库数据、编译缓存和开发日志。

12. 窄屏“功能 → 显示”开关重新区分选中、悬停与未选中状态：选中项增加品牌色描边、加深底色和状态圆点，取消选中后立即恢复白底，修复触屏或鼠标停留导致的伪选中视觉残留。

13. 顶部授权入口仅在“已授权”或“试用已到期”时显示，正常试用期间隐藏以减少导航干扰；功能面板内仍保留授权入口。821px 以下把“更多”从底部导航迁移到顶部生成按钮旁，底部收敛为拖拽、豆笔、镊子、复制和颜色五项高频工具。

14. 顶部授权状态进一步收敛为异常提醒：有效授权时不再显示“已授权”，正常试用同样隐藏；仅试用到期、授权期限到期或生成次数耗尽时展示，授权入口仍可从功能面板随时打开。

15. “更多”面板内圆豆/色块、显示开关和好友联机状态统一使用白底未选中、薄荷绿选中、品牌绿描边及右上状态圆点，并补充 `aria-pressed` 状态；保存、清空和文件操作继续保持各自语义色。

16. 修复好友联机按钮选中后悬停仍触发旧珊瑚色背景、导致深绿色文字对比不足的问题；联机选中态悬停现在保持统一薄荷绿配色并适度加深反馈。

17. 完整颜色筛选弹窗的“已使用”区域按豆子数量降序排列，数量相同时保持原色卡顺序；PC 左侧快捷颜色栏改用稳定队列，初始化时优先装入当前色和已使用颜色，队列内颜色不再参与用量排序。选择已有快捷色保持原位置，仅当当前色不在快捷列表中时插入第一位，避免豆子数量变化或连续切色导致按钮跳动。色号弹窗同时监听画布修订号，修复大图原地更新用量数组后有数量的颜色仍滞留在“未使用”区域的问题。

18. 移动端顶部导航在 360px 及以上宽度恢复显示“拼了个豆”系统名称，并重新分配品牌区与操作区的弹性空间；359px 及以下继续仅显示品牌图标，避免极窄屏按钮和文字拥挤换行。

19. 修复移动端进入全屏后功能面板自动收起、但“功能”悬浮入口同时被隐藏而无法再次展开的问题；全屏顶部按“生成图纸 / 功能 / 退出全屏”从左到右分隔排列，并继续避让刘海与安全区。

20. 修复全屏或横屏切换时响应式状态与 CSS 断点短暂不同步，导致“操作 / 颜色 / 历史”快捷编辑栏在 PC 窄窗口和移动端同时消失的问题。全屏统一挂载快捷编辑栏，并按可用空间自动停靠：常规移动屏使用左侧竖栏，小尺寸、低高度横屏及微信/iOS 强制横屏改用底部横向工具坞；内容过多时支持隐藏滚动条的触控滑动，同时为画布和缩放控件预留对应空间。非全屏继续使用原移动端底部工具栏。

21. 缩放下限改为随画板尺寸、坐标边距和当前可视区域动态计算的“适应画布”比例，进度条、百分比输入、鼠标滚轮与双指缩放统一不能继续缩小到该比例以下，放大上限保持 200%；开发者二维码弹窗新增版本号 `V 0.2.1`。

## 2026-09-10（图纸生成流程收口与跨端可靠性加固）

1. 图纸生成统一为“上传图片 → 选择豆子 → 设置图纸 → 选择创建方式”四步流程，参数面板不再常驻工作台；桌面端使用右侧停靠面板，平板使用居中弹窗，手机使用安全区底部抽屉。
2. 生成表单改为独立草稿：品牌、色卡、底板、尺寸和图像处理参数在用户确认前不再写入当前作品；关闭后重新打开会从当前作品恢复，避免浏览参数时意外清空或重建画布。
3. 编辑器新增原子化 `generate` / `createBlank` 提交：色卡、图像量化或接口失败时保留旧画布、历史记录和参数，仅在全部准备成功后一次性提交新状态。
4. 上传检测与裁剪增加请求序号、取消清理和位图释放，旧异步结果不能覆盖后选图片；自动识别尺寸与待用标题延迟到裁剪、生成成功后再生效。
5. 生成确认增加重复提交保护和完整参数校验；不合理参数会定位到对应步骤，空白画布与图片生成共享同一套草稿设置和错误处理。
6. 响应式切换与全屏状态变化时自动重挂载生成面板，在桌面停靠、平板模态、手机抽屉之间切换不丢草稿；横屏窄设备加入刘海安全区和导航避让。
7. 桌面画布底部恢复“圆豆 / 色块”快速切换，移动端保持原底部工具；全屏回退层级、图标文字间距、极窄屏按钮排列和颜色选择器顶层显示同步校准。
8. 新增生成草稿隔离回归场景，并完成 1440×900、320px 竖屏、移动端横屏及桌面/移动端互转截图验证；页面无横向溢出，生产构建、前端首页与后端健康检查通过。
9. 顶部导航移除当前图纸名称与导入文件名称展示；导入后标题仅保留为保存、导出等业务数据，不再占用导航空间或暴露测试图纸名称。
10. 窄屏底部导航新增常驻“复制颜色”入口，不再收进更多面板；保存按钮使用品牌绿色，清空按钮使用危险红色，与普通功能形成明确的操作语义区分。
11. 开屏退场纳入 2.5 秒总时间轴，遮罩、品牌内容与主工作台改为 420ms 交叉淡入淡出，消除动画结束后的生硬跳帧感；修复全局“减少动态效果”规则将开屏过渡压缩为 0.01ms 的问题，降级模式改用无位移的淡入淡出。

## 2026-09-03（V3 概念稿工作台收口与多端回归）

1. 以概念稿为最高视觉依据重新收口创作工作台：统一奶油纸张背景、森林绿/薄荷绿/珊瑚红主题、轻边框与低对比阴影；顶部品牌、当前图纸、保存状态、功能菜单和核心生成入口重新建立稳定层级。
2. 图纸设置不再常驻桌面端，不占用画板空间；所有生成参数继续由顶部“生成图纸”弹窗承载。桌面端采用三列步骤布局，移动端采用安全区底部抽屉与逐步展开，原生成校验、上传裁剪和生成确认逻辑保持不变。
3. 桌面端左侧工具轨按“操作 / 显示 / 历史”分组，撤销与恢复始终在外部可见；移动端保留拖拽、豆笔、镊子、颜色、更多五项底栏，并把撤销/恢复独立固定在画板左上方。
4. 建立集中式 24×24 单色线性图标系统，统一安全区、圆角线宽、光学中心与按钮基线；生成、菜单、拖拽、豆笔、镊子、复制颜色、显示、文件、联机和弹窗操作均使用同一视觉语言。
5. 色号选择器移入浏览器顶层并根据可视区域计算安全位置；移动端使用可滚动全高面板，解决工具栏裁剪、弹窗遮挡和窄屏按钮文字不完整问题。“更多”面板支持纵向滚动且隐藏滚动条，底部主导航始终保留。
6. 画板维持空白初始状态；圆豆使用俯视空心圆柱与低对比塑料材质，外径等于豆针中心距以保证相邻豆子完全相切。常见 32×32 画板自动适配到清晰尺寸，手动缩放范围仍为 10%～200%。
7. 修复 1024～1180px 区间残留的右侧空列，画布现可完整利用剩余宽度；实测 1440×900、1024×768、821×740、820×740、390×844、340×720 均无页面级横向溢出或操作按钮遮挡。
8. 完成核心交互回归：功能菜单、生成弹窗、移动端更多面板、色号选择面板、撤销/恢复入口与全屏入口均可正常打开；前端 `vue-tsc -b` 与 Vite 生产构建通过，前端首页及后端健康检查均返回正常。
9. 补齐原图上传、工程文件导入和好友联机邀请码输入框的无障碍名称；审查结果无重复 DOM ID、无空名称按钮、无失效弹窗标题引用，25 类业务弹窗在桌面端与移动端均保持在安全可视区域内。

## 2026-08-28（完整界面组件系统）

1. 新增 `frontend/src/studio-components.css` 作为最终加载的界面组件层，以概念稿为最高视觉依据，统一授权、云同步、微信引导、开发者、生成确认、图纸管理、拼接、导入、替换、用料、清空、导出、联机等二十余类弹窗的纸张表面、标题层级、内容滚动区和操作页脚。
2. 建立可直接编码复用的 UI 令牌：纸白、森林绿、薄荷绿、珊瑚红及警告/错误语义色，统一 9/14/20px 圆角、聚焦光环、浮层阴影、模态阴影和动效曲线；表单输入、选择器、卡片、危险操作和禁用状态不再由各功能单独定义。
3. 移动端弹窗统一为安全区底部抽屉，说明文字与操作按钮分层排布；极窄屏按钮保持等宽并排，色号面板进入浏览器顶层且自适应单列/双列，联机浮层避开刘海和左右安全区。
4. 轻提示新增信息、成功、警告、错误和联机五类语义外观与图标；错误条改为可关闭的顶层提示，避免被模态弹窗和悬浮工具遮挡。
5. 联机申请和权限审批通知移除独立内嵌 SVG，改用中央语义图标库；系统常规按钮审计后仅保留品牌入口、页签、尺寸预设、颜色项和缩放数值等必须依赖文本或数据的无图标控件。
6. 熨烫预览的设置浮层、保存引导卡、保存与返回操作同步概念稿的纸张纹理、品牌豆点、图标基线和安全区；真实效果渲染算法与业务逻辑不变。
7. 改造前增量快照位于 `rollback/ui-system-20260828-before.zip`；`vue-tsc -b` 与 Vite 生产构建通过，本地开发服务 `http://localhost:8088/` 返回 200。

## 2026-08-28（概念稿图标系统与布局精修）

1. 新增 `frontend/src/icons/studioIcons.ts`，按统一 24×24 安全区、1.7px 圆角线条和光学中心重画 53 个工作台语义图标；`AppIcon.vue` 改为从集中式图标表渲染，按钮不再散落独立 SVG 或字符图标。
2. “更多”菜单按概念稿缩为右侧属性列内的轻量命令菜单，核心入口优先展示，低频工具、文件和显示项折叠收纳；桌面图纸标题改为真实居中，移动端补齐标题下拉提示。
3. 移动端“更多”面板改为停靠在五项快捷编辑栏上方，保留刘海屏安全区和纵向滚动，不再覆盖拖拽、豆笔、镊子、颜色与更多入口。
4. 色号选择器改用浏览器原生顶层 Popover，并根据 VisualViewport 重新计算位置；弹层可脱离工具栏、模糊背景和滚动容器的裁剪，解决颜色列表被遮挡或显示不完整的问题。
5. 图纸设置补齐形状分段按钮、宽高输入和总孔数反馈，常用调色板的完整色号入口改为概念稿圆形加号；联机入口在命令菜单内恢复统一的轻量列表样式。
6. 改造前增量快照位于 `rollback/ui-redesign-20260828-before-icons.zip`；前端 TypeScript 检查与 Vite 生产构建通过。

## 2026-08-27（年轻化创作工作台 UI 重构）

1. 按概念稿重构桌面端创作工作台：顶栏统一品牌、图纸上下文、保存状态与关键入口；左侧改为分组工具轨，右上“更多”改为轻量下拉菜单，生成参数改为右侧属性抽屉。
2. 按同一视觉体系重构移动端：顶部仅保留品牌豆粒、图纸名和核心动作，快捷编辑进入底部拇指区，更多功能与生成参数使用安全区底部面板，避免窄屏堆叠。
3. 新增独立视觉覆盖层 `frontend/src/studio-redesign.css`，集中维护森林绿、薄荷绿、珊瑚红、奶油白、薰衣草紫等设计令牌；原业务事件、Pinia 数据流、联机和导入导出逻辑保持不变。
4. 重绘第二代 24×24 语义图标，统一圆角线条、光学中心和单色线性语言，覆盖生成、更多、拖拽、豆笔、镊子、颜色、保存、预览、导入导出与联机等主要入口；多色只保留在品牌豆粒标志中。
5. 重构实体豆板渲染：空位改为暖白柱钉与圆形孔位；圆豆按“套在豆针上的空心圆柱体”绘制同色侧壁、哑光顶面、中心孔、孔内豆针和轻接触阴影，移除镜面反光；外径等于豆针中心距，相邻豆子完全相切。
6. 增加常用色卡快捷色轨、缩放加减与适应画布入口，并补齐菜单、抽屉、悬浮工具的按压和过渡反馈；不改变画布编辑约束与自动保存规则。
7. 前端 `vue-tsc -b` 与 Vite 生产构建通过；完成 1365×768 桌面端、390×844 移动端主界面/更多菜单/生成面板视觉回归，浏览器控制台无错误。
8. 改造前快照位于 `rollback/ui-redesign-20260827-before.zip`，回退说明见 `UI_REDESIGN_ROLLBACK.md`。
9. 依据概念稿二次校准主题令牌：品牌绿 `#145C47`、薄荷绿 `#BFE8D8`、珊瑚红 `#FF6B5E`、薰衣草紫 `#C9C4F2`、暖黄 `#F7C95C`、奶油白 `#F7F4EE`；基础样式与视觉覆盖层使用同一组变量，避免旧主题漏出。
10. 快捷编辑按概念稿重新编组：桌面端为操作/颜色/历史三段工具轨，移动端为拖拽、豆笔、镊子、颜色、更多五项底部工具；文件、显示、保存和清空收拢到“更多”，核心生成入口保持同级突出。
11. 熨烫预览的控制栏、保存卡片和操作按钮统一到纸白、品牌绿和珊瑚红主题；深色预览背景仅用于观察透明效果图边缘，不作为交互主题色。
12. 开屏动画同步采用统一主题色与空心圆柱豆模型，豆子按中心距连续相切，保留整体扫光动画但不再给单颗豆子添加镜面高光。
13. 继续按概念稿校准桌面结构：图纸设置由满高抽屉改为右下方常驻属性卡，上方保留“更多”菜单与未来能力空间；顶部动作按保存状态、更多、生成图纸排序，授权状态迁入更多菜单。
14. 图纸设置补齐“颜色 / 图纸 / 显示”三类页签、常用色轨、颜色上限、豆板尺寸和显示开关；桌面端采用非模态常驻卡片，移动端采用带拖拽提示条的底部设置面板，继续复用原生成校验与确认流程。
15. 移动端快捷编辑重排为等宽五入口底部工具栏，并兼容窄屏与安全区；桌面全屏入口改为概念稿同款圆形图标按钮，备案信息退出主布局流，避免压缩画布高度。
16. 再次校准豆子材质：接触阴影限制在单格内部，圆柱顶面与侧壁比例更接近参考稿，采用完整低对比哑光上沿代替局部高光；外径仍严格等于孔距，相邻豆子保持完全相切。
17. 清理常用语义图标的装饰圆点，重新绘制菜单、豆笔、拖拽、镊子、上传、保存、用料、熨烫、云端与对话框图标，使 24×24 线性图标的视觉中心和概念稿一致。
18. 本阶段增量回退快照位于 `rollback/ui-redesign-20260827-phase2-before.zip`；前端 TypeScript 检查与 Vite 生产构建再次通过。

## 2026-08-27（多人联机并发一致性加固）

1. 修复 SSE 已应用较新事件后，较旧 HTTP 编辑/撤销响应仍可能覆盖前端新状态的问题；所有增量统一按服务端序号判定为已应用、重复、过期或缺口。
2. 新增整板 `boardRevision` 版本屏障；清空、替换、重新同步图纸后递增，服务端拒绝基于旧图纸产生的延迟编辑，避免旧下标或旧色号污染新画布。
3. 收到清空、换图或跨版本完整快照时，前端丢弃未发送的旧编辑并清除旧锁显示；检测到序号缺口后停止剩余队列，重连获取完整权威快照。
4. 修复锁清扫与续锁并发时误删新锁的问题；关闭成员编辑权限或成员离开时立即释放对应锁，避免其他成员被无效锁阻塞。
5. 空画布执行清空也会建立新版本屏障，确保清空前排队的编辑不能在清空后重新写回。
6. 新增同格并发唯一胜者、旧图纸延迟编辑拒绝、空画布清空屏障测试；后端 13 项测试及前端生产构建通过。

## 2026-08-26（补充批次：全站语义图标、窄屏响应式体验与 Linux v2 发布包）

### 一、全站语义图标统一
1. 新增统一语义图标组件 `frontend/src/components/AppIcon.vue`，接入此前设计审查稿中的主题图标；缺失的搜索图标按同一套圆角线条、豆粒强调色规范补齐。
2. 审查前端 137 个按钮：除豆板尺寸预设、拼接尺寸预设和画布信息等必须依赖文字/数据的内容按钮外，其余操作入口均使用语义图标；高频快捷编辑操作采用纯图标并补齐 `title`、`aria-label`。
3. 图标尺寸不再继承按钮 `font-size`，按顶部导航、弹窗、文件操作、快捷编辑等使用场景分别设置清晰尺寸，避免文字字号变化导致图标失真或按钮被撑大。
4. 为非对称图标建立 24×24 安全区和光学重心补偿，校准钥匙、生成、菜单、撤销/恢复、拖拽、豆笔、搜索等图形的视觉中心；设计审查页同步展示最终效果。
5. 重新设计开屏“跳过”图标为居中的双箭头结构，强调豆点收进图形内部，修复原图标右上视觉过重的问题。

### 二、按钮与移动端交互优化
1. 修复旧样式把全屏按钮文字当图标放大到 20px 的问题；进入/退出全屏按钮改为紧凑尺寸，SVG 与文字沿同一条 Flex 中线居中。
2. 移动端点击拖拽、豆笔、镊子、复制颜色时，在页面底部安全区上方显示对应功能轻提示；桌面端交互保持不变。
3. 用户可见名称“豆针”统一调整为“豆笔”，同步按钮提示、无障碍名称、移动端轻提示、设计审查页和 README；内部数据字段保持兼容。
4. 功能入口与生成入口仍会自动收起快捷编辑区；本轮仅调整图标、提示和布局，不改变画布编辑、撤销/恢复与联机权限逻辑。

### 三、窄屏响应式布局
1. 屏幕宽度不超过 420px 时，顶部导航隐藏“拼了个豆”品牌文字，仅保留品牌豆粒标志，避免头部操作被挤压换行。
2. 窄屏顶部“生成拼豆图纸”简写为“生成图纸”，按钮无障碍名称仍保留完整语义；实测 339px 宽度无横向溢出。
3. 通用弹窗底部重新规划为“说明文字独占一行、操作按钮下一行等宽并排”；极窄屏按钮缩短内边距，避免取消/确认按钮纵向堆叠或错位。
4. 全屏、弹窗关闭、顶部操作和快捷编辑图标均采用独立尺寸，避免放大图标时同步撑大按钮点击区域。

### 四、回退与验证
1. 改造前源码快照位于 `rollback/icon-refresh-20260826/`，执行其中 `ROLLBACK.ps1` 可恢复 App.vue、styles.css 和相关组件，并移除统一图标组件。
2. 前端 `vue-tsc -b` 与 Vite 生产构建通过；桌面 1440×900、移动端 390×844 与窄屏 339px 完成实际布局检查，无页面横向溢出。
3. 新增 Linux Docker Compose 源码发布包 `release/pindou-studio-linux-20260826-v2.tar.gz`；旧版同日发布包保留，可用于版本回退。

## 2026-08-26（本批次：联机完善、编辑体验、音效、安全加固与 Linux 打包）

### 一、文案与交互
1. 「导出豆板」统一改为「导出图纸」——App.vue（4 处描述）。
2. 成员退出联机增加「确认退出」弹窗，取消不退出、确认才退出——App.vue。
3. 联机成员名称改为「颜色+豆」（蓝豆/黄豆/紫豆/青豆，按成员分配色）——后端 CollabService.cs。
4. 「好友联机」入口在授权关闭时不再被隐藏，授权拦截统一由打开弹窗时的资格校验处理——App.vue。

### 二、联机功能修复
1. 成员编辑权限默认关闭、共享权限默认开启（后端默认值 + 审批下发 canEdit 同步前端）——CollabModels.cs / CollabService.cs / App.vue。
2. 修复房主申请列表倒计时一直为 0（运行旧后端进程所致，重启新二进制后按 expiresAt 正确递减）。
3. 成员关闭网页离线后，房主端自动移除成员信息（SSE 断线超时踢出）——CollabService.cs；离线判定时长 60s→15s，移除更快。
4. 消息提示通知图标及样式：仅联机相关消息显示好友图标，普通消息不显示——App.vue / styles.css。
5. 联机申请超时弹窗改为轻提示（Toast），主动取消不提示——App.vue。
6. 房主离线自动关闭房间，成员端提示「房主已离线」——CollabService.cs。
7. 试用到期不可申请联机、到期成员自动退出（来源 IP 记录 + 周期清扫踢出 trial_expired）——CollabService.cs / CollabModels.cs。
8. 复制链接申请试用与手动输入同走后端 /api/collab/apply 门槛拦截——App.vue / api.ts。
9. 联机提醒图标用主题配色（绿色图标 + 珊瑚边框 + 红徽标提示新申请），修复选中态图标被覆盖为红色——styles.css。
10. 有编辑权限时成员可以取出房主的豆子（移除「成员不能移除房主豆子」限制，清理无用 CellOwner）——CollabService.cs。

### 三、联机安全审计修复（高/中危）
1. 【高危】并发审批同一申请可导致房间超员 → 改为原子抢占（room.Pending.TryRemove 成功者才创建成员）——CollabService.cs。
2. 【高危】清扫任务与审批并发覆盖审批结果 → TryRemove 失败即已被审批抢占，不再推送超时结果——CollabService.cs。
3. 【中危】审批结果轮询端点无鉴权无限流 → 叠加 RequireRateLimiting("api")（60 次/分钟/IP）——Program.cs。
4. 【中危】成员颜色不复用、进出后新成员与房主同色 → 复用 1..4 中未使用颜色——CollabService.cs。
5. 【低危】快照色板透传无大小限制 → 限制 ≤2000；清理死代码 TakeApplyChannel——CollabService.cs。
6. 确认安全项：成员令牌 192 位随机、SSE 票据单次 60s、房主专属操作校验、房间/连接/申请/批量上限、前端模板转义防 XSS。

### 四、数据维护
- 清理本地数据库试用记录（ip_trials 4→0），授权密钥、用户、云存档保留。

### 五、我的图纸与拼接
1. 我的图纸支持重命名，名称不可与其他图纸重复——App.vue。
2. 拼接豆板输入宽度/高度超过 160 自动修正为 160——App.vue。
3. 重命名输入框样式与系统一致（复用保存图纸名称输入框样式）——styles.css。
4. 重命名弹窗说明文字与输入框保持 8px 间距——styles.css。

### 六、画布编辑体验
1. 显示开关（网格/色号/分板线/坐标）移到画布底部信息栏，单行不换行；画板信息过长省略号隐藏、点击弹窗显示完整——App.vue / styles.css。
2. 底部「保存状态」宽度固定（可容纳最长 6 字状态），文字变化时多选区域不左右移动——styles.css。
3. PC 端（鼠标设备）WASD/方向键平移画布（Shift 加速 120px），键盘操控不切换交互模式——PatternCanvas.vue。
4. 滚轮上下始终为缩放（不依赖交互模式，任意工具下滚动即缩放），向上放大、向下缩小——PatternCanvas.vue。

### 七、音效
1. 新增音效：放豆（down.wav）、取出豆子（pick.wav）、打开色号选择器（search.wav）——PatternCanvas.vue / ColorPickerPopover.vue / sound.ts。
2. 底部信息栏多选区域新增「声音」开关，控制音效开/关并持久化到 localStorage——App.vue。
3. 音效 Audio 懒加载，开关关闭或浏览器自动播放限制时静默降级——stores/sound.ts。

### 八、安全审查、性能与打包（本批次收尾）
1. 全面安全审查：确认安全头、限流、并发门控、参数化 SQL、HttpOnly 会话、SSE 票据、图片尺寸/像素限制、admin 常量时间比较、导出限制——基线健全无新增高危。
2. 性能优化：nginx 开启 gzip 压缩（css/js/json/svg）；确认后端 ResponseCompression/数据库池/并发信号量与前端异步组件/静态缓存已配置——nginx.conf。
3. favicon：改用正式 favicon.ico，index.html 引用 /favicon.ico，消除 404——index.html。
4. Linux 部署包（Docker Compose 源码包）：`release/pindou-studio-linux-20260826.tar.gz`（5.27MB），含 backend/frontend 源码 + Dockerfile + docker-compose.yml + deploy-linux.sh + .env.example + certs 说明 + 部署文档；发布包后端独立编译 0 错误 0 警告；已清理敏感文件（admin-token.txt）与冗余（.bak、构建缓存）。

### 九、联机清空（房主清空并广播）
- 需求：房主联机后即可清空豆板（此前联机时清空按钮被隐藏）。已实现「房主可清空并广播」：
  1. 后端新增 POST /api/collab/clear：仅房主可清空，校验后清空房间全部格子与锁，向所有成员广播 clear 事件——Program.cs / CollabService.cs。
  2. 前端：房主联机时快捷工具栏显示「清空豆板」按钮（成员仍只显示「退出联机」）；确认弹窗提示「联机中：将同步清空所有成员的共享画布」；清空时调 collab.requestClear() 广播 + 本地清空——App.vue。
  3. 成员端收到 clear 事件后整体清空本地画布（幂等）——collab.ts / api.ts。
- 验证：API 层成员清空返回 409、房主清空 200；浏览器端到端房主清空 41→0、成员画布同步 41→0（SSE 广播生效）。

### 十、联机增强（30s 申请限制、成员权限申请/审批、授权与试用门槛）
1. 需求1：申请联机 30 秒倒计时，倒计时期间不可再次发起申请；刷新/关闭浏览器倒计时不清空——join 防重复守卫 + 申请按钮倒计时禁用（collab.ts / App.vue）；倒计时经 sessionStorage 持久化、restoreSession 恢复。
2. 需求2：成员点击顶部「联机中」在成员列表中退出（退出图标 + 确认弹窗）——App.vue；成员按缺失权限显示「申请编辑 / 申请共享」按钮（有权限时隐藏，仅可申请自己的权限）——App.vue。
3. 需求2：权限申请流程：成员申请 → 房主收到独立「钥匙图标」铃铛「x豆申请xx权限」→ 同意/拒绝；30 秒申请冷却（前端持久化 + 后端仲裁）、30 秒超时作废；成员离线对应申请消息自动清空——后端 /api/collab/permission-apply、/api/collab/permission-decide（Program.cs / CollabService.cs / CollabModels.cs）+ 前端 collab.ts（permApply/permDecide、SSE perm_apply/perm_decided/perm_removed/perm_expired）。
4. 需求3：后台授权功能关闭（licensingDisabled）后，好友联机按钮同步隐藏、联机不可用——App.vue 入口 v-if + isCollabEntitled 调整。
5. 需求4：试用到期（无密钥）用户好友联机不可用，相关功能按钮隐藏——App.vue 入口 v-if 条件。
6. 修复：房主审批权限申请后铃铛未清理 → 后端审批后补发 perm_removed 事件，房主端铃铛即时清空——CollabService.cs。
7. 增强：权限申请列表带申请者豆子颜色圆点（perm_apply 携带 colorIndex）——CollabService.cs / collab.ts / App.vue。
8. 增强：通知图标晃动改为以中间位置为中心晃动（transform-origin 50% 50%）——styles.css。
- 验证：API 层权限申请/审批/30s 冷却/已拥有权限拒绝均通过；浏览器端到端：申请倒计时与刷新保留、权限按钮显示与冷却、房主钥匙铃铛（含豆色圆点）、同意后铃铛清空、成员收到同意提示、成员退出确认均通过；需求3授权关闭入口隐藏通过。

### 十一、联机同步与只读工具栏
1. 需求A：成员无编辑权限时左侧工具栏隐藏，只显示「申请编辑权限」图标 + 「退出」按钮；点击申请图标直接发送编辑权限申请并轻提示「已向房主申请编辑权限」（30 秒冷却倒计时）——App.vue / styles.css。
2. 需求B：联机状态下房主生成新图纸/替换整块画布后数据同步给联机成员，且成员重新进入联机看到的也是最新数据——后端新增 POST /api/collab/resync：房主推送新快照，服务端更新权威快照并广播 resync 事件（Program.cs / CollabService.cs / CollabModels.cs）；前端 collab.ts（resyncSnapshot + SSE resync 整体替换）、api.ts；App.vue 在生成图纸后自动同步。
- 验证：浏览器端到端——无编辑权限成员只读工具栏仅申请编辑+退出、点击轻提示与 30s 冷却、获权后工具栏恢复；房主 resyncSnapshot 后成员画布同步（18=18），成员重新进入联机仍是最新数据（18，非旧数据）均通过。

### 十二、前端联机授权控制（402 修复）
1. 创建联机房间增加前端授权拦截：未授权（无密钥且试用到期）时直接提示「创建联机房间需要有效授权，请先获取密钥」并引导密钥弹窗，不再向后端发送 /api/collab/host 请求（避免每次 402）——App.vue handleHostCollab。
2. 「创建联机房间」按钮在未授权时禁用（:disabled 纳入 isCollabEntitled()）——App.vue。
3. 打开好友联机入口时若未授权，先刷新一次授权/试用状态再判断，避免后端试用被重置后前端仍显示耗尽而误拦截——App.vue openCollabDialog。
- 验证：浏览器——授权可用时创建房间成功（POST /api/collab/host 返回 200 非 402）；模拟未授权时按钮禁用、点击不发请求、提示文案正确、无 402。

### 十三、联机：成员离开提示、画布替换同步补全、只读工具栏去重
1. 需求1：联机成员退出房间时房主轻提示「x豆已离开房间」——后端 KickMember 广播携带成员名（CollabService.cs），前端房主收到 kicked（他人）时 toast 提示（collab.ts）。
2. 需求2：联机状态下房主整块替换画布后同步给成员，成员重入也是新数据——除上传生成（generate）外，补全所有整块替换画布的同步：新建空白豆板（newBlank）、打开我的图纸（applySavedProject）、导入（applyImportedProject）、云端恢复存档（restoreSharedProject）、微信接力恢复（restoreSharedProject）——App.vue 均调用 collab.resyncSnapshot()。
3. 需求3：联机成员只读左侧工具栏去重——只读区仅保留「申请编辑权限」图标按钮（铅笔图标，无文字不遮挡），退出按钮沿用上方既有按钮不重复——App.vue / styles.css。
- 说明（试用用户在线/离线判断）：成员在线状态基于 SSE 活跃连接（CollabMember.SseCount + OfflineSince），无活跃 SSE 超过 15 秒即视为离线自动移出房间并通知房主；试用用户另有授权判定——无密钥按 IP 试用状态（到期踢出 trial_expired）、有密钥按密钥状态（过期踢出 license_expired）。
- 验证：浏览器——成员退出房主收到「蓝豆 已离开房间」；newBlank/替换画布后成员同步（12×8/6 颗）且重入为最新数据；只读工具栏仅申请编辑图标（38×36px 完整显示）、退出按钮唯一保留于上方。

### 十四、工具栏权限态图标 + 成员申请替换图纸 + 房主审批消息队列倒计时
1. 需求1：成员「有共享权限但无编辑权限」时，左侧只读工具栏显示「保存图标 + 申请编辑权限图标」——App.vue 只读工具栏新增保存按钮（collabCanSave 时显示）。
2. 需求2：成员「有编辑权限但无共享权限」时，将保存图标替换为「申请共享权限」图标（与权限申请同冷却/轻提示逻辑）；共享权限申请成功后自动恢复为保存图标——App.vue / styles.css。
3. 需求3：成员生成图纸 / 用自己的图纸数据（含新建空白）更新整张画布时，改为向房主申请替换：确认弹窗 → 提交后本地恢复联机画布并轻提示 → 房主收到「x豆 申请替换当前图纸」→ 同意后整张图纸同步更新（房主与所有联机成员同步新数据）——后端新增 POST /api/collab/replace-request、/api/collab/replace-decide（Program.cs / CollabService.cs / CollabModels.cs），1 分钟冷却（前端 sessionStorage 持久化 + 后端仲裁）与 1 分钟有效期（超时自动作废）；SSE 事件 replace_apply / replace_removed / resync（前端 collab.ts / api.ts / App.vue）。
4. 需求4：所有带冷却的申请在房主通知消息队列统一展示并带倒计时，超时自动移出队列，倒计时不受刷新影响——联机申请（已有）、权限申请（补充 ExpiresAt 下发并显示倒计时）、替换图纸申请（新队列显示倒计时）统一收进房主「审批消息」钥匙铃铛；pendingPerms / pendingReplaces 纳入房间 DTO，刷新/重连后恢复完整队列与剩余倒计时——CollabService.cs（perm_apply 携带 expiresAt、BuildRoomDto 输出 pendingPerms/pendingReplaces、SweepAsync 超时清扫）/ App.vue / collab.ts / styles.css。
- 验证：浏览器——成员有共享无编辑时只读工具栏显示保存+申请编辑；有编辑无共享时保存位显示申请共享、获权后恢复保存；成员生成图纸弹「申请替换当前图纸」确认框、房主钥匙铃铛显示「蓝豆 申请替换当前图纸」+ 1 分钟倒计时、同意后房主与成员画布同步为新图纸；权限/替换申请队列倒计时刷新后仍正确递减、超时自动移出队列。

### 十四·补充：权限申请房主提醒、队列倒计时兜底与排版
1. 成员申请编辑/共享权限时，房主弹 Toast 轻提示「x豆 申请编辑/共享权限」（与替换申请一致）——collab.ts（perm_apply 事件设置 notice）。
2. 修复「通知铃铛消失」：syncApprovalQueues 仅在房间 DTO 携带 pendingPerms/pendingReplaces 字段时才覆盖队列（旧后端无此字段不再清空已收到的申请）——collab.ts。
3. 修复「消息队列倒计时一直为 0」：事件/DTO 未下发 expiresAt（旧后端）时按等待时长兜底（权限 30s、替换 60s）——collab.ts（resolveExpiresAt）。
4. 倒计时只显示秒数（如 30s），去掉「剩余 X 秒」多余文字——App.vue（collabCountdownText）。
5. 队列文字不用省略号：不强制换行，超出时在文字列内自然换行并左对齐、不与色块圆圈对齐——App.vue / styles.css（collab-apply-name 改 inline-flex + collab-apply-name-text）。
6. 修复「申请替换当前图纸 404」：本地后端进程为旧发布版（无 replace-request 端点）→ 重新发布 `release/run-backend` 并以 ASPNETCORE_URLS=http://localhost:5080 重启，端点验证返回 400（非 404）。
7. 消息队列弹窗宽度自适应内容：`width: max-content; min-width:270px; max-width:min(480px, calc(100vw - 40px))`，完整展示申请信息不截断，小屏封顶防溢出——styles.css。
8. 修复「审批队列超时后延迟移除」：后端清扫周期 20s→3s（CollabSweeper.cs）；前端新增 1s 队列清扫，倒计时归零即本地移除 pendingPerms/pendingReplaces（collab.ts），无需等待后端广播。
9. 权限申请冷却 30s→60s（前端 PERM_APPLY_COOLDOWN_MS + 后端 PermApplyCoolDownTtl，提示文案同步）：冷却期大于 30 秒有效期 + 清扫/网络延迟，避免上一申请未到期就重复发送——collab.ts / CollabService.cs。清扫性能说明：3s 清扫主要为内存态扫描（房间/成员/申请/锁），仅房主密钥与试用按房间做 DB 查询，量级极小；若并发房间数很大可把授权校验改为低频（每 N 轮），当前规模无影响。
10. 修复「刷新后权限申请按钮变可点、实则在冷却且无提示」：服务端拒绝（冷却/已有待审批）时改为 Toast 轻提示（permNotice）而非仅内联文案，并在本地补回 60s 冷却（sessionStorage 持久化，刷新后按钮恢复置灰/倒计时）；handlePermApply 仅提交成功时提示——collab.ts / App.vue。
11. 顶部「联机中」成员列表移除本人「申请编辑 / 申请共享」按钮，仅保留退出联机入口；申请权限统一改由左侧工具栏入口——App.vue。
12. 网页最底部新增页脚：ICP 备案号「桂ICP备2026012053号-1」（点击跳转 https://beian.miit.gov.cn/）+ 版本信息「V0.2.0」——App.vue / styles.css。

### 十五、联机商业级可靠性、安全与本人撤销隔离
1. 新增房间级业务锁，原子化处理容量审批、成员加入、快照替换、格子编辑、清空和编辑序号；修复并发审批突破 5 人上限、编辑序号丢失以及编辑与重同步并发越界。
2. 联机色板由 object 改为强类型契约，限制 2,000 色、字段长度、HEX/RGB/Lab 格式及格子色号范围；编辑接口同步校验色号上界，阻止异常稀疏数组和超大色板造成客户端卡死。
3. 撤销/恢复改为服务端权威的“本人历史”：房主和成员各自独立，每格记录变更版本；他人后续编辑过的格子不会被旧撤销覆盖，支持一笔编辑跨批次合并。
4. SSE 订阅绑定 memberId，单成员最多 3 条连接；踢出、退出和授权到期立即关闭该成员现有通道。队列满时关闭落后连接并通过重连完整 state 恢复，不再静默丢弃编辑事件。
5. 编辑接口改用独立令牌桶限流；前端批处理调整为 320ms，并对 408/429/503/网络异常执行指数退避重试，失败后自动重连恢复权威画布。
6. SSE state/resync/clear/edits 引入连续序号检查；前端发现缺口立即停止应用增量并获取完整快照。currentSnapshot 随编辑和清空实时更新，修复替换申请取消后恢复旧画布。
7. 房间 DTO 按角色裁剪：邀请码和审批队列仅房主可见；替换确认弹窗按 Esc 也执行恢复逻辑。
8. 后台授权复核由每 3 秒逐成员查询降为每成员至少间隔 30 秒，内存态过期清扫仍保持 3 秒；房间关闭和成员离开同步清理冷却、历史及活跃元数据。
9. 源码配置移除真实管理令牌和空密码生产连接串，运行时 admin-token.txt 纳入忽略规则；开发数据库配置移入 appsettings.Development.json，开发 CORS 不再允许任意来源携带凭证。
10. 新增 Pindou.Api.Tests 自动化测试项目，覆盖并发容量、快照校验、重同步竞态、序号完整性、本人撤销隔离、SSE 撤销、DTO 最小化和资源回收。

## 2026-08-28（概念 UI 最终校准与设计稿交付）

1. 新增 `frontend/src/studio-concept.css`，并在 `frontend/src/main.ts` 最后加载。该层以概念 UI 图为最高视觉依据，统一桌面端顶栏、三段工具轨、画板舞台、底部控制条、右侧图纸设置卡，以及移动端顶栏、五项工具栏和安全区底部抽屉；不改变生成、编辑、联机、保存与导出业务逻辑。
2. 建立与代码一一对应的主题令牌：品牌绿 `#145C47`、薄荷绿 `#BFE8D8`、珊瑚红 `#FF6B5E`、薰衣草紫 `#C9C4F2`、暖黄 `#F7C95C`、画布米白 `#F7F4EE`，并补全正文、边框、状态色、圆角、阴影、焦点环与动效变量。
3. `UI_DESIGN_SPEC.md` 补充字体栈和字号层级、4px 间距栅格、六档响应式行为，以及默认、悬浮、按下、聚焦、选中、禁用、加载、成功、警告、错误、联机和只读状态规范。
4. 更新交互式完整 UI 设计稿，覆盖桌面端、移动端、弹窗与浮层、组件状态、语义图标和实现规范，并同步输出到 `design-review/pindou-complete-ui-design.html`。
5. 本轮回退快照：`rollback/ui-concept-recalibration-20260828-before.zip`；前端 TypeScript 检查和 Vite 生产构建通过。
6. 针对“仅有代码映射、看不到图标产物”的问题，新增 `scripts/export-studio-icons.mjs`，从同一语义源导出 56 个独立 SVG、一个 SVG Sprite、图标审查页和高清总览图；资产位于 `frontend/public/icons/studio/`，审查页为 `design-review/button-icon-review.html`。
7. `AppIcon.vue` 改为通过 CSS Mask 直接加载独立 SVG 资产，系统内按钮与交付图标文件使用完全相同的矢量产物，不再只依赖组件内部 `v-html` 绘制。
8. 根据概念图重新校准工作区几何关系：桌面端锁定 74px 三段工具轨、300px 右侧设置卡、188px 更多菜单和居中画布控制条；移动端锁定五项底部工具、70px 安全区工具栏和 24px 底部抽屉。清除多余渐变和半透明漂浮感，恢复概念图的纯纸面、细边框和克制阴影。
9. 用户重新指定横向“品牌色 / 图标风格 / 按钮样式 / 控件样式”规范条为唯一组件参考：图标线宽调整为 1.65px 黑色圆角线稿，主按钮锁定 `#FF6B5E` 与 8px 圆角，开关、单选、复选、分段选择和下拉统一为 `#42A98D`、`#BFE8D8` 与细灰边框。
10. 新增可直接审查的 `design-review/concept-component-strip.svg`、高清 PNG 及重新排版的 `design-review/button-icon-review.html`；审查页不再以卡片化图标墙作为主视觉，首先展示用户指定的横向组件规范条。
11. 进一步统一图标资产：所有独立 SVG 通过 `scripts/verify-studio-icons.mjs` 校验，固定 24×24 画布、2–22 安全区、1.65px 描边、round linecap/linejoin；新增 `frontend/public/icons/studio/README.md` 约束后续新增图标不混用其他风格。
